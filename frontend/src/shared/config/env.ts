// Конфигурация окружения
export const config = {
  // Переключатель: true = использовать mock API, false = использовать реальный API
  USE_MOCK_API: true,
  
  // URL backend API (когда USE_MOCK_API = false)
  API_BASE_URL: 'http://localhost:3000/api',
};